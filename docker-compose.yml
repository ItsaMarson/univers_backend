services:
  mysql:
    image: mysql:latest
    container_name: mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: univers
      MYSQL_ROOT_PASSWORD: Lordmarkpapa25!
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
